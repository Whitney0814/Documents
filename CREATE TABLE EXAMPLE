CREATE TABLE EXAMPLE(
    A NUMBER(10),
    B NUMBER(10),
    C NUMBER(10)
);

DROP TABLE EXAMPLE;

CREATE TABLE EXAMPLE(
    id NUMBER(10) NOT NULL,
    A NUMBER(10),
    B NUMBER(10),
    C NUMBER(10)
);

DROP TABLE EXAMPLE;

CREATE TABLE EXAMPLE(
    id NUMBER(10) NOT NULL,
    A NUMBER(10),
    B NUMBER(10),
    C NUMBER(10),
    CONSTRAINT EXAMPLE_PK PRIMARY KEY (id)
);

CREATE SEQUENCE [시퀀스명]

CREATE SEQUENCE [시퀀스명]
INCREMENT BY [증감숫자]
START WITH [시작숫자]

CREATE SEQUENCE SEQ_EXAMPLE
INCREMENT BY 1
START WITH 1;

'insufficient privileges'


cmd(splplus system/1234)
GRANT CREATE SEQUENCE TO SMC_USER;

CREATE SEQUENCE SEQ_EXAMPLE
INCREMENT BY 1
START WITH 1;

'CREATE SEQUENCE'


SELECT SEQ_EXAMPLE.NEXTVAL FROM DUAL;
-> 1씩 증가

