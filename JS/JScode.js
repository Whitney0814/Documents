// 이 코드는 알림창을 띄우는 코드입니다.
alert( 'Hello, world!' );

/* 변수
var: 몇번 선언해도 에러X
let: 한번만 선언 가능(주 사용)
const: 상수(변하지 않음)
※선언할 때에는 let을 붙여줌.
*/

const PG_FEE = 0.03;

// const scanner = new scanner();

let a = 1;
let b = 2;
let c = "asdf";

let sell_price = 20000;
let pg_fee = sell_price * PG_FEE;

// alert(`${a}, ${b}, ${c}`);
// alert(`${typeof a}, ${typeof b}, ${typeof c}, ${typeof (a + c)}`);

// result = prompt("기본값", "안녕");
// alert(result);

// let isBoss = confirm("Are you the boss?");
// alert( isBoss ); // true if OK is pressed
